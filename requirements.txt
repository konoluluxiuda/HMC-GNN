# ============================================
# HMC-GNN dependencies
# ============================================

# Core ML
torch>=2.0.0
torch-geometric>=2.4.0

# PyG extensions (auto-installed with torch-geometric, pinned for clarity)
torch-scatter
torch-sparse
torch-cluster

# Data processing
numpy>=1.24
pandas>=1.5
scipy>=1.10

# Machine learning utilities
scikit-learn>=1.0
tqdm

# ============================================
# Optional: enable only if using corresponding features
# ============================================

# RDKit — required for chemical fingerprint features (USE_CHEM_FINGERPRINT)
# rdkit

# Transformers / HuggingFace — required for BERT-based features
# (USE_CHEM_DENSE with BERT, USE_DISEASE_TEXT)
# transformers>=4.19.0
# torchvision

# Visualization (fig/ scripts)
# matplotlib
# seaborn
